[pytest]
pythonpath = .
markers =
    slow: tests that require model weights (perception pipeline)
    clevr: CLEVR val integration tests (requires dataset)
    clevr_full: full 10 000-sample CLEVR val run (slow)
testpaths = tests
